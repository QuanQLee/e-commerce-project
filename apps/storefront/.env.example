# Public env vars are baked into the bundle at build time.
# Copy this file to .env.local (ignored) and set real values per environment.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
NEXT_PUBLIC_API_KEY=
NEXT_PUBLIC_TENANT_ID=public

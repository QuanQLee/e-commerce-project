spring:
  datasource:
    url: jdbc:h2:mem:catalog-java;MODE=PostgreSQL;DATABASE_TO_LOWER=FALSE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate

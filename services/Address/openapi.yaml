openapi: 3.0.0
info:
  title: Address API
  version: v1
paths:
  /validate:
    get:
      summary: Validate address
      parameters:
        - in: query
          name: address
          required: true
          schema:
            type: string
      responses:
        '200':
          description: OK
components: {}

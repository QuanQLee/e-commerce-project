openapi: 3.0.0
info:
  title: Tax API
  version: v1
paths:
  /rates/{country}:
    get:
      summary: Get tax rate
      parameters:
        - in: path
          name: country
          required: true
          schema:
            type: string
      responses:
        '200':
          description: OK
components: {}
